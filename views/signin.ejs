<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs') %>
    <title>Sign In - Digital Dynamics Review</title>
    <style>
        body {
            background-color: var(--gray-50);
        }

        .signin-card {
            max-width: 450px;
            margin: 60px auto;
            padding: 30px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        .form-label i {
            margin-right: 6px;
        }
    </style>
</head>

<body>
    <%- include('./partials/nav.ejs') %>

    <div class="container animate-fade-in">
        <div class="signin-card animate-scale-in">
            <h3 class="text-center mb-4 animate-fade-in-down">Sign In to Digital Dynamics Review</h3>

            <!-- Error Message -->
            <% if (error) { %>
                <div class="alert alert-danger text-center" role="alert">
                    <%= error %>
                </div>
            <% } %>

            <form action="/user/signin" method="post">
                <div class="mb-3">
                    <label for="exampleInputEmail1" class="form-label">
                        <i class="bi bi-envelope-fill"></i> Email address
                    </label>
                    <input type="email" name="email" class="form-control" id="exampleInputEmail1" required>
                    <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
                </div>

                <div class="mb-3">
                    <label for="exampleInputPassword1" class="form-label">
                        <i class="bi bi-lock-fill"></i> Password
                    </label>
                    <input type="password" name="Password" class="form-control" id="exampleInputPassword1" required>
                </div>

                <button type="submit" class="btn btn-primary w-100">Sign In</button>
                <p class="mt-3 text-center">
                    Don’t have an account? <a href="/user/signup">Sign up</a>
                </p>
            </form>
        </div>
    </div>

    <%- include('./partials/scripts.ejs') %>
    <!-- Bootstrap Icons CDN (in case it's not already in head.ejs) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</body>

</html>
